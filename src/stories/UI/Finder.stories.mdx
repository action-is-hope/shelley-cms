import { Meta, Canvas, Story, ArgsTable, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import { StoryGrid } from "../StoryLayout";
import { Finder } from "../../components/Finder/Finder";
import { BasicExample } from "./Finder.examples";

<Meta
  title="Finder/Finder"
  argTypes={{
    reviewRequired: {
      control: { type: "boolean" },
    },
  }}
  component={Finder}
  decorators={[(Story) => <Story />]}
/>

# Finder Layout

<Source
  language="tsx"
  code={dedent`
    import { Finder } from "@actionishope/shelley-cms";
  `}
/>

Back to Finder, there really isn't much to look at as I shall now demonstrate:

<Story
  name="a) Finder"
  parameters={{
    docs: {
      source: {
        code: dedent`
TBC`,
      },
    },
  }}
>
  <BasicExample />
</Story>

Check EditorLayout page for proper example in context.

<Source language="tsx" id="editor-contentarea--a-content-area" />

## Content

## Finder Props

<ArgsTable of={Finder} />

## Stying

Add some info...

<Source
  language="css"
  dark
  format={false}
  code={dedent`
    #docs {
      write-some: style-docs;
    }
  `}
/>
