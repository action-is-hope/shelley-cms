import { Meta, Canvas, Story, ArgsTable, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import { StoryGrid } from "../StoryLayout";
import { Finder } from "../../components/Finder/Finder";
import {
  BasicExample,
  DisableActionBarExample,
  NoSidebarExample,
  ContentExample,
  MediaExample,
} from "./Finder.examples";

<Meta
  title="Finder/Finder"
  argTypes={{
    reviewRequired: {
      control: { type: "boolean" },
    },
  }}
  component={Finder}
  decorators={[(Story) => <Story />]}
/>

# Finder

<Source
  language="tsx"
  code={dedent`
    import { Finder } from "@actionishope/shelley-cms";
  `}
/>

Finder is a set of UI components that provide a 'housing' for search and filter related functions. You have two main areas, `content` and `sidebar` both of which have access to `isMobile` for a mobile centric UI tweaks such as displaying fewer colums of a table.

## Example

<Story
  name="a) Finder"
  parameters={{
    docs: {
      source: {
        code: dedent`
<Finder
  title={
    <>
      <ContentIcon aria-hidden="true" />
      Title
    </>
  }
  addButton={
    <Button
      vol={3}
      icon={<Add aria-hidden="true" />}
      onPress={() => console.log("Add action")}
    >
      Add Content
    </Button>
  }
  searchFieldProps={{
    onChange: (value) => console.log(value),
    placeholder: "Search for content",
  }}
  sideBarContent={(isMobile: boolean) => (
    <div>
      <Checkbox>Live</Checkbox>
      {isMobile ? "mobileOn" : "mobileOff"}
    </div>
  )}
>
  <>
    {(isMobile: boolean) => (
      <p>Main Content. {isMobile ? "mobileOn" : "mobileOff"}</p>
    )}
  </>
</Finder>`,
      },
    },
  }}
>
  <BasicExample />
</Story>

<Source language="tsx" id="finder-finder--a-finder" />

### Disable ActionBar

<Story name="b) Disable ActionBar">
  <DisableActionBarExample />
</Story>

[Code: DisableActionBarExample](https://github.com/action-is-hope/shelley-cms/blob/main/src/stories/UI/Finder.examples.tsx)

### No Sidebar

<Story name="c) No Sidebar">
  <NoSidebarExample />
</Story>

[Code: NoSidebarExample](https://github.com/action-is-hope/shelley-cms/blob/main/src/stories/UI/Finder.examples.tsx)

## Content Finder

<Story name="d) Content Finder">
  <ContentExample />
</Story>

[Code: ContentExample](https://github.com/action-is-hope/shelley-cms/blob/main/src/stories/UI/Finder.examples.tsx)

### Media Finder

<Story name="e) Media Finder">
  <MediaExample />
</Story>

[Code: MediaExample](https://github.com/action-is-hope/shelley-cms/blob/main/src/stories/UI/Finder.examples.tsx)

## Finder Props

<ArgsTable of={Finder} />

## Stying

Add some info...

<Source
  language="css"
  dark
  format={false}
  code={dedent`
    #docs {
      write-some: style-docs;
    }
  `}
/>
