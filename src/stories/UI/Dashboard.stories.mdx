import { Meta, Canvas, Story, ArgsTable, Source } from "@storybook/addon-docs";
import dedent from "ts-dedent";
import { StoryGrid } from "../StoryLayout";
import { Dashboard } from "../../components/Dashboard/Dashboard";
import { BasicExample } from "./Dashboard.examples";

<Meta
  title="Dashboard/Dashboard"
  component={Dashboard}
  decorators={[(Story) => <Story />]}
/>

# Dashboard

<Source
  language="tsx"
  code={dedent`
    import { Dashboard } from "@actionishope/shelley-cms";
  `}
/>

Dashboard is essentially the homepage of a CMS and it is comprised on Widgets that are designed to be useful for Content authors.

<Story
  name="a) Dashboard"
  parameters={{
    docs: {
      source: {
        code: dedent`
TBC`,
      },
    },
  }}
>
  <BasicExample />
</Story>

<Source language="tsx" id="editor-contentarea--a-dashboard" />

## Dashboard Props

<ArgsTable of={Dashboard} />

## Stying

Add some info...

<Source
  language="css"
  dark
  format={false}
  code={dedent`
    #docs {
      write-some: style-docs;
    }
  `}
/>
